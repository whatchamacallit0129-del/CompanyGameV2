[project]
name = "companygame-agent"
version = "0.1.0"
description = "Extensible local orchestration layer for CompanyGameV2"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
companygame-agent = "companygame_agent.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
